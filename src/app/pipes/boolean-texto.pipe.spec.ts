import { BooleanTextoPipe } from './boolean-texto.pipe';

describe('BooleanTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
